<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Use cases. Depends on Domain only — never on the generated gateway clients, so the
    mapping code a customer writes never has to know CoverGo speaks GraphQL.
  -->

  <ItemGroup>
    <ProjectReference Include="..\CoverGo.Samples.Domain\CoverGo.Samples.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>

</Project>
