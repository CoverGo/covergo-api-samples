Closes #

## What and why

<!-- What changes, and why. The diff shows what; explain the reasoning and any trade-off. -->

## Verified against a tenant

<!-- Unit tests prove the mapping; only a real call proves the sample.
     What did you run, and what came back? -->

- [ ] Ran it against a CoverGo tenant
- [ ] Ran it a **second time** and it skipped rather than duplicating
- [ ] `npm run build`, `npm run lint` and `npm run test` pass

## Nothing internal

- [ ] No credential, token, tenant URL or customer data is committed
- [ ] No CoverGo client is named or identifiable anywhere in the diff, including sample data,
      comments and the commit messages
- [ ] No internal tracker key — this repository tracks work in its own GitHub issues
- [ ] Any new setting has an obviously-fake placeholder in `appsettings.json`

## Known gaps

<!-- Anything deliberately left out, and why. A gap written down is useful;
     one discovered later is not. Write "none" if there are none. -->
