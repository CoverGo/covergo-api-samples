name: Setup
description: Install Node and restore the Node and .NET dependencies for the samples workspace.

runs:
  using: composite
  steps:
    - name: Install Node
      uses: actions/setup-node@v7
      with:
        node-version: 24
        cache: npm

    - name: Install Nx
      run: npm ci
      shell: bash

    - name: Restore .NET dependencies
      run: dotnet restore CoverGo.Samples.slnx
      shell: bash
